<?xml version="1.0" encoding="utf-8"?>
<!--
    扫描页：
      - 整页是一层半透明遮罩（#80000000）
      - 中间悬一张白色大圆角卡片：
          * 标题「扫描音乐」+ 右上角旋转加载圈
          * 一列正在被扫描/已扫描的文件名（逐首追加到 LinearLayout）
          * 底部「确定」主按钮：扫描中禁用灰色，扫描结束后蓝色可点
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#80000000">

    <LinearLayout
        android:id="@+id/scanCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:orientation="vertical"
        android:background="@drawable/scan_card_bg"
        android:padding="24dp">

        <!-- 标题 + 加载圈 -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/tvScanTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start|center_vertical"
                android:text="扫描音乐"
                android:textSize="22sp"
                android:textStyle="normal"
                android:textColor="#FF000000" />

            <ProgressBar
                android:id="@+id/scanSpinner"
                style="?android:attr/progressBarStyle"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="end|center_vertical"
                android:indeterminateTint="#FF1E5AC0" />
        </FrameLayout>

        <!-- 文件名滚动列表 -->
        <ScrollView
            android:id="@+id/scanScroll"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="18dp"
            android:layout_marginBottom="16dp"
            android:scrollbars="none"
            android:fadeScrollbars="false"
            android:overScrollMode="never">

            <LinearLayout
                android:id="@+id/scanList"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </ScrollView>

        <!-- 确定按钮：扫描中灰，扫完蓝 -->
        <TextView
            android:id="@+id/btnScanConfirm"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:gravity="center"
            android:text="确定"
            android:textSize="16sp"
            android:textStyle="normal"
            android:textColor="#FF888888"
            android:background="@drawable/scan_btn_disabled_bg"
            android:clickable="false"
            android:focusable="false" />

    </LinearLayout>

</FrameLayout>
